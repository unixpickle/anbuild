part of anbuild;

/**
 * A target which runs in a separate isolate.
 */
class _TargetTask {
  final String targetDirectory;
  
  _TargetTask(this.targetDirectory);
  
  Future _createPackages() {
    
  }
  
  Future _runIsolate() {
    
  }
  
  Future _deletePackages() {
    
  }
}
