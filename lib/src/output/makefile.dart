part of anbuild;

Future exportMakefile(String targetRoot, TargetResult target) {
  // TODO: here, use $(CXX), $(CC), $(CXXFLAGS), and $(CCFLAGS) to generate a
  // makefile which generates objects and does lots of magical things...
}
